# pime-config

Phase 2（PIME 整合）預留目錄，將存放向 PIME 註冊自製 backend 所需的設定檔
（`ime.json` 等），指定 PIME 改用本專案的 `zuyin-backend` 而非官方 Python 範例
後端。

目前尚未進入 Phase 2，此目錄先保留位置，實際內容待研究 PIME 官方文件與範例
後端的設定格式後補上（見 `docs/PROJECT_PLAN.md` 三、分階段開發規劃）。
