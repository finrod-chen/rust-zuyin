[package]
name = "zuyin-backend"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Named-pipe backend service that bridges PIME and zuyin-core"

[[bin]]
name = "zuyin-backend"
path = "src/main.rs"

[dependencies]
zuyin-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
